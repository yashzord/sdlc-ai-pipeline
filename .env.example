# Google AI Studio key — free tier: https://aistudio.google.com/apikey
# Without a key the app runs in demo mode with realistic pre-scripted outputs.
GEMINI_API_KEY=

# Optional: override the model (defaults to gemini-2.5-flash with fallbacks)
GEMINI_MODEL=
